#include <iostream>
#include "BST_Tree.hpp"

int main()
{
    BST_program program;
    program.setupRiver(); // loads and prints
    program.showMenu();   // runs the user interaction loop
    return 0;
}